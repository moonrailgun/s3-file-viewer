# SSH隧道功能使用说明

## 功能概述

SSH隧道功能允许您通过SSH连接访问内网中的Minio实例，这对于在安全环境中部署的存储服务特别有用。

## 配置步骤

### 1. 启用SSH隧道

在连接表单中，找到"SSH隧道配置"部分，勾选"启用SSH隧道"复选框。

### 2. 配置SSH连接信息

- **SSH主机**: SSH服务器的地址（例如：ssh.example.com）
- **SSH端口**: SSH服务器的端口（默认：22）
- **SSH用户名**: 用于SSH连接的用户名

### 3. 选择认证方式

#### 密码认证
- 选择"密码认证"
- 输入SSH密码

#### 密钥认证
- 选择"密钥认证"
- 输入私钥文件路径（例如：/Users/username/.ssh/id_rsa）
- 如果私钥有密码保护，输入私钥密码

### 4. 配置目标服务器信息

- **远程主机**: Minio服务器在内网中的地址（通常是localhost或内网IP）
- **远程端口**: Minio服务器的端口（默认：9000）

## 工作原理

1. 应用程序建立到SSH服务器的连接
2. 创建本地端口转发，将本地端口映射到远程Minio服务器
3. S3客户端连接到本地端口，流量通过SSH隧道转发到远程服务器

## 使用场景

- 访问位于私有网络中的Minio服务器
- 通过跳板机访问内网存储服务
- 在安全隔离环境中进行文件管理

## 注意事项

1. 确保SSH服务器允许端口转发（AllowTcpForwarding yes）
2. 私钥文件需要有正确的权限设置（600）
3. 如果使用密钥认证，确保公钥已添加到目标服务器的authorized_keys中

## 故障排除

如果连接失败，请检查：
- SSH服务器地址和端口是否正确
- 用户名和密码/密钥是否正确
- 目标Minio服务器地址和端口是否可达
- 防火墙设置是否允许相关端口
